name: Scrape Tech Company Jobs (Using External Ollama)

on:
  schedule:
    - cron: '0 6,18 * * *'  # 6:00 AM and 6:00 PM UTC
  workflow_dispatch:  # Manual trigger

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # Reduced from 120 since we don't need to install Ollama

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # OLLAMA IS NOW RUNNING ON ORACLE CLOUD VM
      # No need to install - just connect to external instance

      - name: Scrape Tier 1 — Greenhouse companies (29)
        id: tier1
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          USE_OLLAMA: true
          OLLAMA_URL: ${{ secrets.OLLAMA_URL }}  # Your Oracle Cloud Ollama URL
          OLLAMA_MODEL: llama3.2:1b  # Use smaller/faster model
        run: npx tsx scripts/scrape-tier.ts --tier=1 --timeout=600000

      - name: Scrape Tier 2 — Lever + Workday companies (22)
        id: tier2
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          USE_OLLAMA: true
          OLLAMA_URL: ${{ secrets.OLLAMA_URL }}
          OLLAMA_MODEL: llama3.2:1b
        run: npx tsx scripts/scrape-tier.ts --tier=2 --timeout=400000

      - name: Scrape Tier 3 — Custom career pages (21)
        id: tier3
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          USE_OLLAMA: true
          OLLAMA_URL: ${{ secrets.OLLAMA_URL }}
          OLLAMA_MODEL: llama3.2:1b
        run: npx tsx scripts/scrape-tier.ts --tier=3 --timeout=200000

      - name: Cleanup stale jobs (>15 days)
        id: cleanup
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx tsx scripts/scrape-tier.ts --cleanup --days=15

      - name: Report Status
        if: always()
        run: |
          echo "=== Scraping Results ==="
          echo "Tier 1 (Greenhouse): ${{ steps.tier1.outcome }}"
          echo "Tier 2 (Lever+Workday): ${{ steps.tier2.outcome }}"
          echo "Tier 3 (Custom): ${{ steps.tier3.outcome }}"
          echo "Cleanup: ${{ steps.cleanup.outcome }}"
          echo "======================="
          if [[ "${{ steps.tier1.outcome }}" == "failure" && "${{ steps.tier2.outcome }}" == "failure" && "${{ steps.tier3.outcome }}" == "failure" ]]; then
            echo "::error::All scrape tiers failed!"
            exit 1
          fi
